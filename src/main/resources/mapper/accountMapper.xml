<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.bit.backpackers.mapper.accountMapper">
  	<select id="selectOne" resultType="com.bit.backpackers.mypage.account.model.entity.AccountVo">
  		select * from tbl_member where userId=#{userId}
  	</select>
  	<update id="updateOne">
  		update tbl_member set
  			userPw=#{userPw},
  			userEmail=#{userEmail},
  			postCode=#{postCode},
  			address=#{address},
  			detailAddress=#{detailAddress},
  			extraAddress=#{extraAddress}
  		where userId={#userId}
  	</update>
  	<delete id="deleteOne">
  		delete from tbl_member where userId=#{userId} and userPw=#{userPw}
  	</delete>
<!--   	<select id="checkPw" resultType="int">
  		select count(*) from tbl_member
  		where userId=#{userId}
  		and userPw=#{userPw}
  	</select> -->
  </mapper>